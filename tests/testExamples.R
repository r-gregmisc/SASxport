library(SASxport)
Sys.setenv("TZ"="GMT")

# run the examples, comparing output
example(SASformat)
example(lookup.xport)
example(makeSASNames)
example(read.xport)
example(toSAS)
example(write.xport)


